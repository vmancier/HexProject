package Tests;

import static org.junit.Assert.*;

import Model.*;
import org.junit.Test;

import javax.swing.*;
import java.awt.*;

public class ModelTest {
    /*
    @Test
    public void testDisplayCell() {
        JFrame jf = new  JFrame();
        Cell c = new Cell(10,20,10,10);
        assertEquals(true, c.displayCell(jf.getGraphics()));
    }

    @Test
    public void testDisplayGrid() {
        JFrame jf = new  JFrame();
        Grid g = new Grid(5,5);
        assertEquals(true,g.displayGrid(jf.getGraphics()));
    }
    */
}
